import { Request, Response } from "express";
import { eventHint } from "./getEventHints";


// Gun ( BlueBox ) && Boss ( Sorrawit )

// ลบ event hint

const deleteEventhint = (req:Request,res:Response<eventHint>)=>{

}

export default deleteEventhint